
const PlaceholderReducer = (state = {}, action) => {
    switch(action.type){
        case 'placeholder':
            return state;
        
    }
    return state;
}

export default ({
    PlaceholderReducer,

});