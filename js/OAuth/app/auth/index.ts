
export * from './authRoute';


/*
export * as routes from '...'
//------------------------------------------------

const authRoutes = require('./auth');
export default authRoutes;

//------------------------------------------------
app.use('/auth', authRoutes);
export default app;


//------------------------------------------------
const authRoutes = require('./auth');
signUp
signIn
signOut



const routes = (app) => {
    app.get('/', (req, res) => { res.send('home'); });
    
    app.use('/auth', authRoutes);
}

module.exports = routes;



const routes = [{
    path:'signup',
    middlewares:[],
    route: signUp
}]





*/